# Croatian Fiscalization Configuration
# Copy these variables to your .env.local file

# Fiscal Provider Selection
# Options: 'mock' (development), 'ie-racuni' (production)
FISCAL_PROVIDER=mock

# IE-Računi Configuration (if using ie-racuni provider)
IE_RACUNI_API_KEY=your_api_key_here
IE_RACUNI_API_URL=https://api.ie-racuni.hr/v1
IE_RACUNI_SANDBOX=true

# Alternative: FINA eRačun Configuration (for future implementation)
# FINA_API_KEY=your_api_key_here
# FINA_API_URL=https://api.fina.hr/v1
# FINA_SANDBOX=true

# Certificate Configuration (for production fiscalization)
# FISCAL_CERTIFICATE_PATH=/path/to/certificate.pfx
# FISCAL_CERTIFICATE_PASSWORD=your_certificate_password

# Notes:
# 1. For development, use FISCAL_PROVIDER=mock
# 2. For production, obtain credentials from IE-Računi or FINA
# 3. Test in sandbox mode first before going live
# 4. Certificates are required for real fiscalization (obtained from FINA)
